/**
 * ClientInsightsPage — narrative analytics (placeholder).
 *
 * Real implementation from client-calls-insights: inline AI readings on
 * every chart, pipeline forecast, vertical benchmark, anomaly timeline with
 * resolution status.
 */
import React from 'react';
import ClientPagePlaceholder from './_ClientPagePlaceholder';

const ClientInsightsPage: React.FC = () => (
  <ClientPagePlaceholder
    title="Insights"
    description="The numbers and what they mean — no naked charts."
  />
);

export default ClientInsightsPage;
